start mongo
mongod --config /usr/local/etc/mongod.conf

import data if it disappears/// path to mongo folder for json
mongoimport --host=127.0.0.1 --db test --collection precincts --type json --file precincts.json --jsonArray
